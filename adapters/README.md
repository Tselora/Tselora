# adapters

Translate framework activity into the Universal Agent Event Protocol.

Scaffolded: `python/` (decorators/context managers) and `otel/` (OpenTelemetry bridge).

Later: LangGraph, OpenAI Agents SDK, CrewAI, Google ADK, MCP. Framework objects must not leak into `core`.
