# ui

React + TypeScript execution inspector (React Flow, WebSockets).

Planned Week 3: graph, timeline, node inspector, basic visualization replay.

The UI renders **projected state**. It must not reconstruct execution semantics or treat arrival order as causality. Bootstrap via REST; live updates via state patches.
